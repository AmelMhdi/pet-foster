{
  "name": "pet-foster",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm run build",
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start"
  }
}